# Working agreements

- Use only synthetic fixtures and public market facts in this repository.
- Do not search for, discover, or read private workspaces outside this root.
- Require an explicit `--workspace` for every CLI operation.
- Keep MOEX/RUB and the current normalized data formats as the v1 scope.
- Add behavior changes test-first and run the offline suite before committing.
- Do not add dependencies without explicit approval.
